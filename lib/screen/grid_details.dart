import 'package:flutter/material.dart';

class GridDetails extends StatelessWidget {
  const GridDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Grid Details');
  }
}
